name: release-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pip:
    - setuptools
    - twine==4.0.0
    - portalocker~=1.2
    - setuptools_rust
    - pytest
    - pytest-xdist
    - pytest-sugar
    - pytest-timeout
    - pytest-mock
    - azure-keyvault
    - azure-identity
