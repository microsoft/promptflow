# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: flows_standard_basic
on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  auto_generated_steps:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Setup Python 3.9 environment
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"
      - name: Prepare requirements
        working-directory: examples/flows/standard/basic
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Prepare dev requirements
        working-directory: examples
        run: |
          python -m pip install --upgrade pip
          pip install -r dev_requirements.txt
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Create Python Environment
        working-directory: examples/flows/standard/basic
        run: |
          echo AZURE_OPENAI_API_KEY=${{ secrets.AOAI_API_KEY }} > .env
          echo AZURE_OPENAI_API_BASE=${{ secrets.AOAI_API_ENDPOINT }} >> .env
          echo AZURE_OPENAI_API_TYPE=azure >> .env
          echo AZURE_OPENAI_API_VERSION=2023-03-15-preview >> .env
      - name: Create run.yml
        working-directory: examples/flows/standard/basic
        run: |
          gpt_base=${{ secrets.AOAI_API_ENDPOINT }}
          gpt_base=$(echo ${gpt_base//\//\\/})
          sed -e "s/\${azure_open_ai_connection.api_key}/${{ secrets.AOAI_API_KEY }}/g" -e "s/\${azure_open_ai_connection.api_base}/$gpt_base/g" run.yml > run.yml
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pip install -r requirements.txt
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          cat .env
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pf flow test --flow .
          pf flow test --flow . --inputs text="Hello World!"
          pf flow test --flow . --node llm --inputs prompt="Write a simple Hello World program that displays the greeting message when executed."
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pf run create --flow . --data ./data.jsonl --stream
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pf run list
          name=$(pf run list -r 1 | jq '.[:1] | .[] | .name' | tr -d '"')
          pf run show --name $name
          pf run show-details --name $name
          pf run visualize --name $name
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pf connection create --file azure_openai.yml --set api_key=${{ secrets.AOAI_API_KEY }} api_base=${{ secrets.AOAI_API_ENDPOINT }}
          pf connection show -n azure_open_ai_connection
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pf flow test --flow . --environment-variables AZURE_OPENAI_API_KEY=${{ secrets.AOAI_API_KEY }} AZURE_OPENAI_API_BASE=${{ secrets.AOAI_API_ENDPOINT }}
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pf run create --flow . --data ./data.jsonl --stream --environment-variables AZURE_OPENAI_API_KEY=${{ secrets.AOAI_API_KEY }} AZURE_OPENAI_API_BASE=${{ secrets.AOAI_API_ENDPOINT }}
          pf run create --file run.yml --stream
          name=$(pf run list -r 1 | jq '.[:1] | .[] | .name' | tr -d '"')
          pf run show-details --name $name
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          az account set -s ${{ secrets.TEST_WORKSPACE_SUB_ID }}
          az configure --defaults group=${{ secrets.TEST_WORKSPACE_RG }} workspace=${{ secrets.TEST_WORKSPACE_NAME }}
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pfazure run create --flow . --data ./data.jsonl --environment-variables AZURE_OPENAI_API_KEY=${{ secrets.AOAI_API_KEY }} AZURE_OPENAI_API_BASE=${{ secrets.AOAI_API_ENDPOINT }} --stream --runtime demo-mir
          pfazure run create --file run.yml --stream --runtime demo-mir
      - name: Bash Execution
        working-directory: examples/flows/standard/basic
        run: |
          pfazure run list -r 3
          name=$(pfazure run list -r 1 | jq '.[:1] | .[] | .name' | tr -d '"')
          pfazure run show --name $name
          pfazure run show-details --name $name
          pfazure run visualize --name $name