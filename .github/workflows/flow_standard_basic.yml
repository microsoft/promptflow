# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: flow_standard_basic
on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  auto_generated_steps:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Setup Python 3.9 environment
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"
      - name: Prepare requirements
        working-directory: examples/flows/standard/basic
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Create Python Environment
        working-directory: examples/flows/standard/basic
        run: |
          echo AZURE_OPENAI_API_KEY=${{ secrets.AZURE_OPENAI_API_KEY }} > .env
          echo AZURE_OPENAI_API_BASE=${{ secrets.AOAI_API_ENDPOINT }} >> .env
          echo AZURE_OPENAI_API_TYPE=azure >> .env
          echo AZURE_OPENAI_API_VERSION=2023-03-15-preview >> .env
      - name: Bash Execution
        run: pf flow test --flow .
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: pf flow test --flow . --inputs text="Hello World!"
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: pf flow test --flow . --node llm --inputs prompt="Write a simple Hello World program that displays the greeting message when executed."
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: pf run create --flow . --data ./data.jsonl --stream
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: name=\$(pf run list | jq '.[:1] | .[] | .name' | tr -d '"') && pf run show --name $name && pf run show-details --name $name && pf run visualize --name $name
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: pf flow test --flow . --environment-variables AZURE_OPENAI_API_KEY=${{ '{{' }} secrets.AZURE_OPENAI_API_KEY }} AZURE_OPENAI_API_BASE=${{ '{{' }} secrets.AOAI_API_ENDPOINT }}
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: pf run create --flow . --data ./data.jsonl --stream --environment-variables AZURE_OPENAI_API_KEY=${{ '{{' }} secrets.AZURE_OPENAI_API_KEY }} AZURE_OPENAI_API_BASE=${{ '{{' }} secrets.AOAI_API_ENDPOINT }}
        working-directory: examples/flows/standard/basic
      - name: Create run.yml
        working-directory: examples/flows/standard/basic
        run: |
          sed -e "s/\${azure_open_ai_connection.api_key}/${{ secrets.AZURE_OPENAI_API_KEY }}/g" -e "s/\${azure_open_ai_connection.api_base}/${{ secrets.AOAI_API_ENDPOINT }}/g" run.yml > run.yml
      - name: Bash Execution
        run: pf run create --file run.yml --stream
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: az account set -s 96aede12-2f73-41cb-b983-6d11a904839b
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: az configure --defaults group="promptflow" workspace="promptflow-eastus"
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: pfazure run create --flow . --data ./data.jsonl --environment-variables AZURE_OPENAI_API_KEY=${{ '{{' }} secrets.AZURE_OPENAI_API_KEY }} AZURE_OPENAI_API_BASE=${{ '{{' }} secrets.AOAI_API_ENDPOINT }} --stream --runtime demo-mir
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: pfazure run create --file run.yml --stream --runtime demo-mir
        working-directory: examples/flows/standard/basic
      - name: Bash Execution
        run: name=\$(pf run list -r 3 | jq '.[:1] | .[] | .name' | tr -d '"') && pfazure run show --name $name && pfazure run show-details --name $name && pfazure run visualize --name $name
        working-directory: examples/flows/standard/basic