# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: flows_evaluation_classification_accuracy_eval
on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  auto_generated_steps:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Setup Python 3.9 environment
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"
      - name: Setup Pandoc
        uses: r-lib/actions/setup-pandoc@v1
        with:
          pandoc-version: '2.12'
      - name: Prepare requirements
        working-directory: examples/flows/evaluation/classification-accuracy-eval
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Prepare dev requirements
        working-directory: examples
        run: |
          python -m pip install --upgrade pip
          pip install -r dev_requirements.txt
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Extract Steps
        working-directory: ${{ github.workspace }}
        run: |
          python scripts/ghactions_driver/extract_steps_from_readme.py -f examples/flows/evaluation/classification-accuracy-eval/README.md -o examples/flows/evaluation/classification-accuracy-eval
      - name: Run scripts
        working-directory: examples/flows/evaluation/classification-accuracy-eval
        run: |
          bash bash_script.sh ${{secrets.AOAI_API_KEY }} ${{ secrets.AOAI_API_ENDPOINT }} ${{ secrets.TEST_WORKSPACE_SUB_ID }} ${{ secrets.TEST_WORKSPACE_RG }} ${{ secrets.TEST_WORKSPACE_NAME }}
      - name: Upload artifact
        if: ${{ always() }}
        uses: actions/upload-artifact@v3
        with:
          name: artifact
          path: examples/flows/evaluation/classification-accuracy-eval/bash_script.sh