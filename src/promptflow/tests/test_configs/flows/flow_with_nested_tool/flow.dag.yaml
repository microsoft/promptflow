inputs:
  input:
    type: string
    default: Hello
outputs:
  output:
    type: string
    reference: ${nested_tool.output}
nodes:
- name: nested_tool
  type: python
  source:
    type: code
    path: nested_tool.py
  inputs:
    input: ${inputs.input}
