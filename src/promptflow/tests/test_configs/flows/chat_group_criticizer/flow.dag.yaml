inputs:
  words:
    type: string
  third_party_comments:
    type: string
  chat_history:
    type: list
outputs:
  result:
    type: string
    reference: ${criticizer.output}
nodes:
- name: criticizer
  type: python
  source:
    type: code
    path: criticizer.py
  inputs:
    words: ${inputs.words}
    third_party_comments: ${inputs.third_party_comments}
    chat_history: ${inputs.chat_history}
